//Temporary parsec.gradle file for testing
apply from: '${applyFromPath}'

group = '${projectGroup}'

buildscript {
    //additional buildscript components
}

repositories {
    //additional repositories
}

dependencies {
    //additional dependencies
}